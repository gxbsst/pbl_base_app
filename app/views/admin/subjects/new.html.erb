<h3>
  新建学科
  <%= link_to '返回列表', admin_subjects_path %>
</h3>
<div class="hr"></div>
<%= form_for @subject, :as => :subject, :url => admin_subjects_path, html: {class: 'form-horizontal'} do |f| %>
    <%= render 'admin/subjects/form', :f => f %>
<% end %>