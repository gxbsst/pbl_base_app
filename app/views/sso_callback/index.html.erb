<script type="text/javascript" charset="utf-8">
    var top = window.top,
            angular = top.angular,
            scope = angular.element('body').scope();
    top.$httpProvider.defaults.headers.common['X-CSRF-Token'] = '<%= form_authenticity_token %>';
    scope.$apply(function () {
        scope.logged = true;
    });
</script>